package lambda.Expressions;
/**
*
* @author Naveen
*/

// linked class VarCapture.
//An example of capturing a local variable from the enclosing scope.
public interface MyFunc {
    int func(int n);
}
